# PostgreSQL Database URL (REQUIRED)
# This is the ONLY database configuration needed.
# The connection string automatically handles SSL for Neon and other remote databases.
#
# For LOCAL DEVELOPMENT with Neon (recommended):
#   DATABASE_URL=postgresql://user:password@region.neon.tech/foodbridge?sslmode=require
#
# For LOCAL DEVELOPMENT with local PostgreSQL:
#   DATABASE_URL=postgresql://postgres:password@localhost:5432/foodbridge
#   DATABASE_URL=postgresql://foodbridge_user:password@localhost:5432/foodbridge
#
# For VERCEL PRODUCTION:
#   Set this environment variable in Vercel project settings
#   Use your Neon PostgreSQL URL (available from https://console.neon.tech)
#
DATABASE_URL=postgresql://user:password@host:port/database

# Node Environment
NODE_ENV=development

# Server Port
PORT=5000
